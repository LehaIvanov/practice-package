{
    "files.exclude": {
        "**/.git": true,         // this is a default value
        "node_modules": true,    // this excludes the folder 
                                // only from the root of
                                // your workspace 
        "dist": true
    }

}